name: Test

on:
  pull_request:
  push:
    branches:
      - '**'
    paths-ignore:
      - '**/README*.md'
      - '**/BUILDING.md'
      - '.github/FUNDING.yml'
      - '.github/ISSUE_TEMPLATE/**'

permissions:
  contents: read # to fetch code (actions/checkout)

jobs:
  test:
    name: Run tests
    runs-on: ubuntu-24.04

    steps:
      - uses: actions/checkout@v6

      - name: Free up disk space in runner
        uses: ./.github/actions/disk-cleanup

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 21

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6

      - name: Copy Docker gradle.properties
        run: cp gradle.properties.docker "$GRADLE_USER_HOME/gradle.properties"

      - name: Build and run tests
        # KIDS MDM IM: scoped to the one variant this fork actually ships
        # (see CI_BUILD_VARIANTS=prodStoreRelease in release.yml). Plain
        # `./gradlew build` builds+tests+lints every flavor combination
        # upstream Molly defines (store/website x prod/staging x debug/
        # release/instrumentation, ~7 variants) even though we only ever
        # ship one, which was turning every push into a 1h+ CI run.
        run: ./gradlew testProdStoreDebugUnitTest

      - name: Archive reports for failed build
        if: "failure()"
        uses: actions/upload-artifact@v7
        with:
          name: reports-${{ github.run_id }}
          path: |
            **/build/reports
            **/build/test-results
          if-no-files-found: ignore
